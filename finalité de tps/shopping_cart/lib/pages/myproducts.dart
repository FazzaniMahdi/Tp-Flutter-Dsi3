import 'package:shopping_cart/widgets/mydiapos.dart';
import 'package:flutter/material.dart';

class Myproducts extends StatelessWidget {
  const Myproducts({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Mydiapos(),
    );
  }
}